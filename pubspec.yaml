name: value_localization_core
description: A lightweight Dart internationalization (i18n) library with JSON-based translations, async loading, and runtime language switching.
version: 1.0.0
repository: https://github.com/ValueNotify666/value_localization_core
homepage: https://github.com/ValueNotify666/value_localization_core

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
